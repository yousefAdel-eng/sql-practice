# sql-practice

A structured SQL & Python practice repository focused on data engineering fundamentals.

## Structure
- `sql/` – SQL exercises (joins, CTEs, window functions)
- `src/` – Python scripts for loading and processing data
- `data/` – Raw and processed datasets
- `docs/` – Notes and weekly plans

## Goal
Build strong foundations in SQL and Python for data engineering workflows.
